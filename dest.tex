lemma main:
  assumes   a1:"s ∈ reachableSet { mutualIni  N} (rules N)"  and
a2:"0<N"
  shows "∀inv. inv ∈(invariants N)⟶formEval inv s"

definition invariants::"nat ⇒ formula set"  where [simp] :
"invariants N≡ {f. exTwoLessP N (% i j.  f = inv1 i j)  ∨
exLessP N (% i.  f= inv2 i)  ∨
exTwoLessP N (% i j.  f = inv3 i j)  ∨
exLessP N (% i.  f= inv4 i)  ∨
exTwoLessP N (% i j.  f = inv5 i j)   }"


lemma NI_Remote_PutXVsInv1:
   assumes   a1:"iRule1 ≤ N" and  a2:"iInv1 ≤ N" and  a3:"iInv2 
≤ N" and  a4:"iInv1~=iInv2  "

   shows  "invHoldForRule (inv1  iInv1  iInv2 ) (NI_Remote_PutX
iRule1 ) 
(invariants   N) (is "?P1∨?P2∨?P3)"

